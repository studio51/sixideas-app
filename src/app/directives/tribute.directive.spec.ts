import { TributeDirective } from './tribute.directive';

describe('TributeDirective', () => {
  it('should create an instance', () => {
    const directive = new TributeDirective();
    expect(directive).toBeTruthy();
  });
});
